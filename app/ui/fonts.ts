import { Inter, Lusitana, Playwrite_GB_S } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
export const playwrite = Playwrite_GB_S({
  weight: ['400'],
//   subsets: ['latin'],
});
